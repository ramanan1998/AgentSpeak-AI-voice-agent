# Pipecat 1.x — browser (SmallWebRTC) + phone (FastAPI WS) voice pipeline.
pipecat-ai[cartesia,deepgram,openai,runner,silero,webrtc,websocket]>=1.3.0,<2

# Vobiz telephony serializer — separate PyPI package; provides pipecat.serializers.vobiz
pipecat-vobiz>=0.0.3,<0.1

# Web framework / server
fastapi~=0.115
uvicorn[standard]~=0.30
python-multipart            # only if you handle form/multipart uploads

# HTTP + utils
httpx~=0.27
python-dotenv~=1.0

# --- confirm these against database.py / storage.py ---
sqlmodel~=0.0.22            # keep if models.py uses SQLModel; else drop
sqlalchemy[asyncio]~=2.0    # async engine/session used in database.py
asyncpg~=0.30               # async Postgres driver
minio~=7.2                  # OR boto3 — whichever storage.py imports